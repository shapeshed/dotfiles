unbind-key j
unbind-key k
bind-key j down
bind-key k up
bind-key ^D pagedown
bind-key ^U pageup
bind-key g home
bind-key G end
bind-key h quit


browser             $BROWSER
color background   default   default
color listnormal   default   default
color listfocus    black     yellow
color listnormal_unread blue default bold
color listfocus_unread black yellow 
color info         default   black
color article      default   default

# highlights
# highlight article "^(Title):.*$" blue default
highlight article "https?://[^ ]+" red default
highlight article "\\[image\\ [0-9]+\\]" green default
download-path       "~/music/podcasts/%h/%n"

goto-first-unread   yes
goto-next-feed      yes
show-read-feeds     no
text-width          80

feed-sort-order     unreadarticlecount-asc

player "mpv"

macro r set browser "tmux split-window && tmux send-keys 'rtv %u && tmux kill-pane\n'"; open-in-browser ; set browser "$BROWSER %u"
macro y set browser "mpv %u"; open-in-browser ; set browser "$BROWSER %u"
macro d set browser "youtube-dl %u"; open-in-browser ; set browser "$BROWSER %u"
macro o set browser "w3m"; open-in-browser ; set browser $BROWSER

